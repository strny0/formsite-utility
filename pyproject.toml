[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "aiofiles",
    "aiohttp",
    "openpyxl",
    "pandas",
    "python_dateutil",
    "pytz",
    "regex",
    "tqdm"
]
build-backend = "setuptools.build_meta"